enum ModuloType {    
    DoubleHashing,
    LinearProbing,
    PrimeSum,
    QuatraticProbing,    
}

export interface HashTable {
    table: any[]
}

const getModulo = (type: ModuloType): number => {

}

export const insertData = (data: any, table?: HashTable): HashTable => {
    
}

export const searchData = (): HashTable => {

}

export const deleteData = (): HashTable => {

}